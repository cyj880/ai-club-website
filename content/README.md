# 课程内容维护

课程目录统一配置在 `course-catalog.json`。其中课程、章节和作业的 `id` 一旦开始收作业便不可修改，否则历史提交无法与任务对应。

每章正文使用 UTF-8 Markdown 文件。支持标题、段落、无序/有序列表、引用、链接、行内代码和代码块；原始 HTML 会被转义，不会直接执行。

暂未提供正文的章节可以保留目录项而不创建对应 Markdown 文件，学习页会显示“资料整理中”。
